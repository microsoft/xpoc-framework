{
  "name": "xpoc-framework",
  "version": "0.3.0",
  "type": "module",
  "description": "Cross-Platform Origin of Content (XPOC) framework",
  "main": "lib/dist/index.js",
  "types": "lib/dist/index.d.ts",
  "scripts": {
    "lint": "eslint **/*.{ts,js,md} --fix",
    "prettier": "prettier --write './**/*.{ts,js,md}'",
    "build:lib": "cd lib && npm install",
    "build:browser-extension": "cd samples/browser-extension && npm install && npm run build:clean",
    "build:client-side-html": "cd samples/client-side-html && npm install && npm run build",
    "build:server-backend": "cd samples/server-backend && npm install && npm run build",
    "build:all": "npm run build:lib && npm run build:browser-extension && npm run build:client-side-html && npm run build:server-backend",
    "prepare": "npm run build:lib"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^6.5.0",
    "@typescript-eslint/parser": "^6.5.0",
    "eslint": "^8.48.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-import": "^2.28.1",
    "eslint-plugin-markdown": "^3.0.1",
    "eslint-plugin-prettier": "^5.0.0",
    "prettier": "^3.0.3"
  }
}